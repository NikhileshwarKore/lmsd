@isTest
private class ExportDataFromMetadataTest {
      @isTest
	  private static void loadFieldData(){
     test.startTest();
        ExportDataFromMetadata expData = new ExportDataFromMetadata();
     test.stopTest();
 }
}